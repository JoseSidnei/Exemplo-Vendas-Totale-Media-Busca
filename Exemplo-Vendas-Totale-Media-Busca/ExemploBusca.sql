-- Busca os produtos que contém o nome indicado com cala
-- SELECT * FROM produtos WHERE nome LIKE 'Cala%';

-- Buscar os produtos que contém o nome finalizando com Calabresa
-- SELECT * FROm produtos WHERE nome LIKE '%Calabressa';

-- Buscar os produtos que contem calabresa em qualquer parte
-- SELECT * FROM produtos WHERE nome LIKE '%Calabresa%';